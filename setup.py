from setuptools import setup

setup(name='gym_pomdp',
      version='0.0.1',
      description='Gym extension for POMDP',
      python_requires='>=3.5',
      install_requires=['gym', 'pygame', 'numpy'])
